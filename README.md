# Object-detection-
**Project Description (300 characters):**  Developed an Object Detection system using TensorFlow's Object Detection API and the SSD Inception V2 model to identify and classify multiple objects in images. The project uses the COCO dataset labels and visualizes detection results with bounding boxes. 
